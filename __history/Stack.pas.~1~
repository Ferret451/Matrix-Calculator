unit TStack;

interface

Type
  PStackOfStringSegment = ^TStackOfStringSegment;
  TStackOfStringSegment = record
    Value: String;
    Next: PStackOfStringSegment;
  end;

Type
  TStackOfString = class

  Private
    FTop: PStackOfStringSegment;
    FSize: integer;

  Public
    Constructor Create;
    Destructor Destroy; override;
    Procedure Push(Value: String);
    Function Top: String;
    Function Pop: String;
    Function Size: integer;
    Function IsEmpty: boolean;

  end;

implementation
  Constructor TStackOfString.Create;
  begin
    FSize := 0;
    FTop := nil;
  end;

  Destructor TStackOfString.Destroy;
  begin
    while not IsEmpty do
      Pop;
    inherited;
  end;

  Procedure TStackOfString.Push (Value: String);
  Var
    Segment: PStackOfStringSegment;
  begin
    New(Segment);
    Segment.Value := Value;
    Segment.Next := FTop;
    FTop := Segment;
    Inc(FSize);
  end;

  Function TStackOfString.Top: String;
  begin
    Result := FTop.value;
  end;

  Function TStackOfString.Pop: String;
  Var
    Segment: PStackOfStringSegment;
  begin
    Segment := FTop;
    Result := Segment.Value;
    FTop := Segment.Next;
    Dispose(Segment);
    Dec(FSize);
  end;

  Function TStackOfString.Size: integer;
  begin
    Result := FSize;
  end;

  Function TStackOfString.IsEmpty: boolean;
  begin
    if (FSize = 0) then
      Result := True
    else
      Result := False;
  end;
end.



